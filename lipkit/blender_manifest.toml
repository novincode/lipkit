schema_version = "1.0.0"

id = "lipkit"
name = "LipKit"
tagline = "Universal Lip Sync for 2D & 3D"
version = "0.1.0"
type = "add-on"

maintainer = "LipKit Team <contact@lipkit.dev>"
license = ["SPDX:MIT"]

blender_version_min = "4.2.0"

# Tags as simple list (required format)
tags = ["animation", "rigging", "lip-sync", "grease-pencil"]

[permissions]
network = "Access phoneme extraction API"
files = "Read audio files and write presets"
clipboard = "Copy/paste mapping presets"
